
# ── Python ──────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
*.egg

# ── Virtual environments ─────────────────────────────────
.env
.venv
env/
venv/
ENV/

# ── Conda ────────────────────────────────────────────────
.conda/

# ── Jupyter ──────────────────────────────────────────────
.ipynb_checkpoints/
*.ipynb_checkpoints

# ── Data files (never commit actual data to Git) ─────────
# Ignore data files but always allow .gitkeep placeholders
data/raw/*.xlsx
data/raw/*.csv
data/raw/*.parquet
data/raw/*.zip
data/raw/*.feather
data/interim/*.csv
data/interim/*.parquet
data/interim/*.feather
# Ignore all processed outputs by default…
data/processed/*.csv
data/processed/*.parquet
data/processed/*.feather
# …except the two Parquet files required by the Streamlit app
!data/processed/customer_360.parquet
!data/processed/clean_transactions.parquet

# ── Model artifacts (binaries excluded; JSON metadata tracked) ───────────────
models/*.pkl
models/*.joblib

# ── macOS system files ───────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride

# ── VS Code ──────────────────────────────────────────────
.vscode/
*.code-workspace

# ── Logs ─────────────────────────────────────────────────
*.log
logs/

# ── Generated chart images (large binaries) ──────────────
reports/figures/*.png
reports/figures/*.jpg
reports/figures/*.html

# ── Screenshots (add manually with git add -f if needed) ─
reports/screenshots/*.jpg

# ── Loose data files dropped in project root ─────────────
*.xlsx
*.zip

# ── Secrets / credentials ────────────────────────────────
*.env
secrets.json
credentials.json
service_account*.json


# Keep README dashboard screenshots
!reports/screenshots/
!reports/screenshots/*.png
!reports/screenshots/README_screenshot_checklist.txt



# ============================================================
# CFPB CX ANALYSIS DATA — DO NOT COMMIT RAW DATA
# ============================================================

data/cx/
reports/cx_nlp/
reports/cx_statistics/
reports/cx_statistics_v2/

# Local Streamlit backup
app/streamlit_app_backup.py
